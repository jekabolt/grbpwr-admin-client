// Чистые правки текста панели форматирования — мерная часть, собирается format-edits-probe.mjs.
//
// Зонд проверяет ровно то, что можно проверить без браузера: `(text, start, end) => Edit`.
// Каретка, стопка отмены и `execCommand` остались в `format-bar.tsx` и сюда не попадают —
// у них другой прибор (стенд), и смешивать их значило бы не проверить ни то ни другое.
export * from 'components/managers/files/note/format-edits';
