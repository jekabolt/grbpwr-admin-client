// Migrated into the shared kit at ui/form/fields/segmented-field.tsx (single-select counterpart
// to ToggleGroupField). This file could not be deleted (sandbox file-delete permission was
// declined) and nothing in src/ imports it anymore — it only re-exports the kit component so it
// can't drift into a second implementation. Safe to remove entirely once permitted.
export { default } from 'ui/form/fields/segmented-field';
