/**
 * ШАПКА СТУДИИ И НЕДОСТАЮЩИЕ ЭТАЖИ ARTIFACTS — одна дверь на шесть органов.
 *
 * Каждый из них — ЧИСТЫЙ орган: он читает форму и полосу, рисует себя и не знает, кто и в каком
 * порядке его смонтирует. Композиция — не здесь: этот бочонок не собирает страницу, чтобы не стать
 * вторым местом, где записан порядок органов студии (первое и единственное — `design/studio-tab.tsx`).
 */
export { AnnotationStrip, ANNOTATION_CHIPS_H } from './annotation-strip';
export { ConstructionDraft } from './construction-draft';
/* `MoodDraft` УДАЛЁН ВМЕСТЕ С ФАЙЛОМ (фича 9). Он предлагал ПРОЗУ в одно поле (`concept`), режа
   `output_text` по трём заголовкам; его место занял `ConstructionDraft`, который просит у того же
   платного прогона СТРУКТУРУ и раскладывает её предложением на четыре группы CONSTRUCTION.
   Оставленная копия читала бы ответ, которого этой дверью больше не просят: та же кнопка, тот же
   `client_request_id`, и ноль заголовков в JSON — то есть пустой черновик БЕЗ ЕДИНОЙ ОШИБКИ на
   оплаченном прогоне. `CONCEPT_MAX` не пропал — он вернулся к своему полю, в `mood-board.tsx`. */
/* `ArtifactsChecklist` и `ArtifactsDraft` УДАЛЕНЫ — они были мёртвым кодом, и это проверено, а не
   предположено: во всём `src/` на них ссылались РОВНО две строки — их собственные определения и
   экспорты этого бочонка, а сам бочонок не импортирует никто (`grep -rn "/head'" src/` пуст;
   `MoodDraft` и `AnnotationStrip` берут по прямому пути). То есть ни один экран их не монтировал.
   Знание из них не пропало, потому что ОБА органа прототипа живут в `design/artifacts-panel.tsx`
   и живут там лучше: `artifactsChecklistHtml` («чего ждёт минт») — это `NoVersionsYet`, у которого
   каждая строка ещё и дверь; `artifactsDraftHtml` (лист до первой версии) — это первый этаж самой
   панели, который рисует плиты и до минта, и после него, одним органом вместо двух состояний.
   Копии, оставленные «на всякий случай», расходятся с живым экраном молча — что здесь и произошло:
   удалённый `ArtifactsChecklist` писал «the first print, callout or release mints v1», тогда как
   живая панель держит противоположное правило и объясняет его — версия рождается ТОЛЬКО от акта
   (печать или релиз), а правка выноски после v1 новой версии не требует. Экран, который никто не
   монтирует, невозможно опровергнуть глазами: он врал бы ровно до того дня, когда его включат. */
/* `StudioHead` и `RepsStrip` УДАЛЕНЫ, и это решение, а не недоделка.
   `StudioHead` был вторым набором контролов для тех же имён формы: карточная шапка уже стоит
   первым рядом СТУДИИ (`index.tsx`, `topRowHtml` прототипа). Два редактора одного поля — не
   удвоение труда, а тихая потеря: `brand`, `collection`, `season`, `target gender` и `fit`
   пишутся НЕ через `UpdateTechCard`, а `StyleFactsField`-ом отдельной командой, и копия показывала
   бы «сохранено», молча возвращая старое значение после перезагрузки.
   `RepsStrip` был строкой делегирования в `KindsStrip` — она и есть полоса представлений
   (`repsStripHtml`). Знание из него не пропало: замороженный счётчик указаний, на который он
   указывал, починен в самом `kinds-strip.tsx`. */
export { draftIdeaRefusal, useDraftDesignIdea } from './use-draft-idea';
