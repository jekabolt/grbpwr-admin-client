/**
 * THE PLAYGROUND — the second aside of the studio. One export: the screen. Everything else
 * (`model.ts`, `drafts.ts`, the groups) is this folder's own business, and a re-export without a
 * reader is a promise of stability nobody pays for.
 */
export { PlaygroundStudio } from './studio';
